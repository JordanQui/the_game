# @vercel/analytics déclare SvelteKit en peer optionnel : npm 11 le résout quand même
# et bute sur vite 8 contre le vite 7 de Nuxt (ERESOLVE). Sans ce réglage, npm
# install échoue aussi sur Vercel.
legacy-peer-deps=true
